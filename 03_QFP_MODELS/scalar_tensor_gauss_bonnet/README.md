# Scalar-Tensor-Gauss-Bonnet

**Object type: WORKING MODEL.** Scalar-tensor-Gauss-Bonnet gravitational sector.

**Object type: ASSUMPTION.** Keep `F(Phi)` and `H(Phi)` independent unless a derivation establishes otherwise.
